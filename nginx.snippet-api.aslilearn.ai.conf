# Deprecated: use the full file instead:
#   deploy/nginx-api.aslilearn.ai.conf
#
# Do NOT rely on a global 300s timeout alone — exam PDF extraction and AI
# batches need the dedicated location blocks in that file (1800s).
#
# If you only need the directives inside an existing server { } block:
client_max_body_size 100M;
proxy_read_timeout 300s;
proxy_connect_timeout 60s;
proxy_send_timeout 300s;
