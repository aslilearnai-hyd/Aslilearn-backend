node_modules/
**/node_modules/

# Environment — never commit secrets
.env*
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.backup
.env*.backup
!.env.example

# Runtime uploads & local env backups (production server)
uploads/
.env.bak.*

# Local Atlas snapshot restore workspace (large binaries)
scripts/_restore-work/
# Local MongoDB recovery snapshots and binaries (never commit)
scripts/_dps-restore-*/
scripts/_restore-work/
